plugins {
    id 'com.android.library'
    id 'maven-publish'
}

android {
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion

    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode rootProject.ext.versionCode
        versionName rootProject.ext.versionName
        consumerProguardFiles "consumer-rules.pro"
    }

    buildTypes {
        release {
            minifyEnabled false
            buildConfigField "String", "VERSION_NAME", "\"${LOCAL_VERSION_NAME}\""
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            buildConfigField "String", "VERSION_NAME", "\"${LOCAL_VERSION_NAME}\""
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    lintOptions {
        checkReleaseBuilds false
        abortOnError false
        ignoreWarnings true
    }
}

dependencies {
    compileOnly rootProject.ext.Official_Library["appcompat"]
    compileOnly rootProject.ext.Tripartite_Library["reflex"]
}

afterEvaluate {
    publishing {
        publications {
            // Creates a Maven publication called "release".
            release(MavenPublication) {
                from components.release
                groupId = 'com.github.CoderAlee'
                artifactId = 'PaintedSkin'
                version = '3.3.3'
            }
        }
    }
}

//uploadArchives {
//    repositories {
//        mavenDeployer {
//            pom.groupId = "org.alee.component"
//            pom.artifactId = "paintedskin"
//            pom.version = "3.0.3"
//            repository(url: "http://192.168.21.220:8081/repository/Component/") {
//                authentication(userName: "peix", password: "peix")
//            }
//            snapshotRepository(url: "http://192.168.21.220:8081/repository/maven-snapshots") {
//                authentication(userName: "peix", password: "peix")
//            }
//        }
//    }
//}
//
//publish {
//    userOrg = 'l15040565660'  //Bintray用户名
//    repoName = 'Alee' //Bintray上创建的仓库名,如果你创建的是maven,则可以干掉这一行,否则需要指定仓库名
//    groupId = 'org.alee.component' //依赖名implementation 'x:y:z'中的x
//    artifactId = 'paintedskin' //依赖名implementation 'x:y:z'中的y
//    publishVersion = '3.1.2' //依赖名implementation 'x:y:z'中的z
//    desc = '换肤核心包' //对这个库的描述
//    website = 'https://github.com/CoderAlee/PaintedSkin' //VCS地址,填写该项目的Github地址就好
//}